name: Codex Review (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "OpenAI API-based PR review is disabled."
          echo "Use Codex GitHub code review for pull request reviews instead."
